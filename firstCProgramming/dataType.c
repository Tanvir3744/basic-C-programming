#include <stdio.h>
#include <math.h> // this header is used for mathmatical operation
int main(){
    int rahim = 10;
    int karim = 12;
    float mohammad = 3.4;
    printf("%d%d", karim, rahim);
    return 0;
}

/*
notes for data type :
 1. there are four kind of data types in c programming
 such as, integer, float, character, boolean,
 2. if we want to print an integer we should use foramte specifier like %d for integer,
    %f -> fload, %c -> for character printing;
 3. to make some mathmatical operation should use math.h header 
 4. 

 */