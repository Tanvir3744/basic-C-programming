#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    char a[1000];
    scanf("%s", &a);
    int length = strlen(a);
    printf("%d", length);

    return 0;
}